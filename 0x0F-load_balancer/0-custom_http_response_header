#!/usr/bin/env bash
# add_header directive to the server when http header requested

sudo apt-get update
sudo apt-get -y install nginx
sudo service nginx start

sed -i "40i\        add_header X-Served-By $hostname;" /etc/nginx/sites-enabled/default

sudo service nginx restart
sudo service nginx reload
